/*
import {ComponentProps} from "react";
import Error from "next/error";

const UserPage = ({page}: ComponentProps<any>) => {

    if (!page) {
        return (
            <Error statusCode={404}/>
        );
    }

    return (
        <>
            <h1>User ID: {page.userId}</h1>
        </>
    );
}

export default UserPage;*/
